Flask==3.0.3
# boto3 >= 1.36 required for Bedrock Knowledge Base "managedSearchConfiguration"
# (older SDKs only know the legacy vectorSearchConfiguration and cannot
# retrieve from managed knowledge bases).
boto3==1.43.88
gunicorn==22.0.0
Werkzeug==3.0.3
